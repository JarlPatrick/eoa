# Required: Root password for MariaDB
MYSQL_ROOT_PASSWORD=

# Required: Password for the 'eoamanager' database user
MYSQL_PASSWORD=

# Optional: Port to expose PHP service on host (default: 8080)
# Leave empty to use the default port 8080
PHP_PORT=

# Optional: Port to expose MariaDB service on host (default: 3306)
# Leave empty to use the default port 3306
DB_PORT=